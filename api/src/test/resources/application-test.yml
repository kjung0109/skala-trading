# 테스트 전용 설정.
#
# 봇과 호가 시딩을 끄는 이유:
# 둘 다 켜져 있으면 테스트가 시작할 때 이미 호가가 있고, 실행 중에도 계속 체결이 일어난다.
# 그러면 "10주를 걸고 10주를 사면 정확히 10주가 체결된다" 같은 검증이 매번 다른 결과를 낸다.
# 테스트는 자기가 만든 호가만 보고 판단할 수 있어야 한다.
app:
  bot:
    enabled: false
  demo:
    seed-order-book: false

logging:
  level:
    org.hibernate.SQL: warn
